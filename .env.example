# Copy to .env and fill in. Get API ID/hash at https://my.telegram.org
# Generate the session strings with get_session.py (first account) and
# qr_login.py (second account).

# First account (Charlotte)
API_ID=
API_HASH=
SESSION_STRING=

# Second account (Vazira)
API_ID_V=
API_HASH_V=
SESSION_STRING_V=
